/**
 * Configuration object for URL.
 * @typedef {Object} URLConfig
 * @property {string} url - The URL value.
 */

/**
 * The URL configuration object.
 * @type {URLConfig}
 */
const urlconfig = {
    url: 'http://localhost/mciyuan/src/'
};

/**
 * Configuration object for URL.
 * @typedef {Object} URLConfig2
 * @property {string} url - The URL value.
 */

/**
 * The second URL configuration object.
 * @type {URLConfig2}
 */
const urlconfig2 = {
    url: 'https://'
};

export { urlconfig, urlconfig2 };
